import { ARO } from "../utils/constantes";

// A cesta fica sempre no mesmo ponto da quadra — a dificuldade vem da boca da
// cesta ficar menor (ver dificuldadeDaRodada).
export function posicaoDoAro() {
  return ARO;
}